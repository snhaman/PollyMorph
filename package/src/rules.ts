// Auto-generated from PollyMorph.json v1.4.0
// Design usage rules — enforced guidelines for color, typography, layout, components, and spacing.

import type { PollyMorphRules } from "./types";
// eslint-disable-next-line @typescript-eslint/no-require-imports
const raw = require("../pollymorph.json");

/** The full rules object **/
export const rules: PollyMorphRules = raw.rules;

// ── Color rules ───────────────────────────────────────────────────────────────
export const rulePrimaryPurple = raw.rules.color.brand.primary_purple as string;
export const rulePrimaryOrange = raw.rules.color.brand.primary_orange as string;
export const ruleNonBrandAccents = raw.rules.color.brand.non_brand_accents as string;
export const ruleStatusYellow = raw.rules.color.brand.status_colors.yellow as string;
export const ruleStatusRed = raw.rules.color.brand.status_colors.red as string;
export const ruleStatusGreen = raw.rules.color.brand.status_colors.green as string;
export const ruleStatusBlue = raw.rules.color.brand.status_colors.blue as string;
export const ruleSidebarBg = raw.rules.color.surface.sidebar_bg as string;
export const ruleSidebarStroke = raw.rules.color.surface.sidebar_stroke as string;
export const rulePlatformSurface = raw.rules.color.surface.platform_surface as string;
export const ruleCardBg = raw.rules.color.surface.card_bg as string;
export const ruleInteractiveLinks = raw.rules.color.text.interactive_links as string;
export const ruleMutedText = raw.rules.color.text.muted_supporting as string;

// ── Typography rules ──────────────────────────────────────────────────────────
export const ruleFontInter = raw.rules.typography.font_families.inter as string;
export const ruleFontSpaceGrotesk = raw.rules.typography.font_families.space_grotesk as string;
export const ruleFontMono = raw.rules.typography.font_families.jetbrains_mono as string;
export const ruleFontMixing = raw.rules.typography.font_families.mixing_rule as string;

// ── Layout rules ──────────────────────────────────────────────────────────────
export const rulePageStructure = raw.rules.layout.page_structure as string;
export const ruleCards = raw.rules.layout.cards as string;
export const ruleTables = raw.rules.layout.tables as string;

// ── Component rules ───────────────────────────────────────────────────────────
export const ruleButtonPrimary = raw.rules.components.buttons.primary as string;
export const ruleButtonSecondary = raw.rules.components.buttons.secondary as string;
export const ruleTabs = raw.rules.components.tabs as string;
export const ruleSidebarActiveState = raw.rules.components.sidebar_active_state as string;
export const rulePagination = raw.rules.components.pagination as string;
